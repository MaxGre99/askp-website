export type EventType = {
	id: string;
	title: string;
	slug: string;
	description: string;
	image?: string;
	published: boolean;
	authorId: string;
	eventDate: string;
	createdAt: string;
	updatedAt: string;
};
